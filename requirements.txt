# Phase 1 / Step 1: Environment Configuration
# Install with:  pip install -r requirements.txt
# (add --break-system-packages on Debian/Ubuntu system Python, or use a venv)

numpy>=1.26
pandas>=2.2
scipy>=1.11
scikit-learn>=1.4          # F1 / AUCROC metrics
torch>=2.2                 # LSTM / Dense Autoencoder (Step 4)
statsmodels>=0.14          # STL seasonal-trend decomposition (Step 5)
pyyaml>=6.0                # config loading
matplotlib>=3.8            # result plots for the dissertation
tqdm>=4.66                 # progress bars
