# Install with: pip install -r requirements.txt --break-system-packages

numpy>=1.26
pandas>=2.2
scipy>=1.11
scikit-learn>=1.4          # metrics: F1, AUCROC, MinMaxScaler
torch>=2.2                 # LSTM / Dense Autoencoder
statsmodels>=0.14          # Seasonal-Trend decomposition (STL)
pyyaml>=6.0                # config loading
matplotlib>=3.8            # plots for the dissertation results chapter
tqdm>=4.66                 # training progress bars
